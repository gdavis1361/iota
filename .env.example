# Core Application Settings
# ======================

# Application Identification
APP_NAME=iota
APP_VERSION=1.0.0

# Environment Configuration
# Valid values: development, testing, staging, production
# Required: Determines configuration and security rules
ENVIRONMENT=development

# Debug Mode
# WARNING: Must be false in production
DEBUG=false

# Security Settings
# ================

# Secret Key
# Required: Must be at least 32 characters long
# SECURITY WARNING: Keep this secret and never commit to version control
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-minimum-32-characters-long

# Allowed Hosts
# JSON array format
# SECURITY WARNING: In production, specify exact hostnames, no wildcards
ALLOWED_HOSTS=["localhost", "127.0.0.1"]

# Logging Configuration
# ===================
LOG_LEVEL=INFO  # Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=json  # Valid values: json, console
CORRELATION_ID_HEADER=X-Correlation-ID

# Sentry Error Tracking
# ====================
# Enable for production error monitoring
SENTRY_ENABLED=false
# Required if SENTRY_ENABLED=true
# Format: https://<key>@<organization>.ingest.sentry.io/<project>
SENTRY_DSN=
SENTRY_ENVIRONMENT=${ENVIRONMENT}
SENTRY_DEBUG=false
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1
SENTRY_MAX_BREADCRUMBS=100
SENTRY_METADATA={"environment": "${ENVIRONMENT}", "region": "us-east-1"}

# Performance Monitoring
# ====================
# Thresholds for performance monitoring and alerting
SLOW_REQUEST_THRESHOLD_MS=1000.0  # Milliseconds
ERROR_RATE_THRESHOLD=0.1          # 0-1 range
SLOW_RATE_THRESHOLD=0.1           # 0-1 range
SAMPLING_WINDOW_SECONDS=60        # Time window for rate calculations
